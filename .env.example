# Upstream provider keys. Never commit real values.
NVIDIA_API_KEY=
OPENROUTER_API_KEY=

# Optional inbound auth for calls into richard-router.
# If set and referenced by config/router.yaml, clients must send Bearer auth.
RICHARD_ROUTER_API_KEY=
